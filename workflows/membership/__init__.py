"""
Membership module for GroupMembership business logic.

The GroupMembership *model* remains in workflows.models to avoid migration
changes.  This package contains the service layer, validators, sync
utilities and custom exceptions that operate on that model.
"""
